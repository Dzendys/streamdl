services:
  streamdl:
    build: .
    container_name: streamdl
    ports:
      - "8082:8080"
    environment:
      - PORT=8080
      - HOST=0.0.0.0
      - YTDLP_COOLDOWN=600
      - COOKIES_FILE=cookies.txt
      - MAX_STREAM_TIMEOUT=60.0
    restart: unless-stopped
